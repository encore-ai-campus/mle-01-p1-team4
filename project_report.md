# 사내 규정 문서 기반 RAG 챗봇 데이터 분석 보고서 (LXpert)

## 1. 분석 개요

### 1.1 프로젝트 개요

본 프로젝트는 사내 규정 문서를 기반으로 사용자의 질문에 관련 규정을 검색하고 답변을 생성하는 **RAG(Retrieval-Augmented Generation) 챗봇 구축**을 목표로 한다.

전체 프로젝트 파이프라인은 다음과 같다.

> 원본 PDF 수집 → 문서 파싱 →  청킹 및 전처리 → 임베딩 → Vector DB 적재 → 검색(Retrieval) → LLM 답변 생성 → 검색 및 답변 품질 평가 → Streamlit 서비스 구현

### 1.2 분석 목적

본 분석의 주요 목적은 다음과 같다.

* 사내 규정 문서 데이터의 기본 구조 및 품질 확인
* 문서별·조문별 데이터 분포 확인
* 메타데이터의 결측 및 이상값 확인
* 문서 간 구조적 차이 확인
* 분석 결과를 기반으로 RAG 전처리·청킹·검색 전략 수립

---

# 2. 데이터셋 개요

## 2.1 원본 데이터

| 구분         | 내용         |
| ---------- | ---------- |
| 데이터 종류     | 사내 규정 PDF  |
| 문서 수       | 8 |
| 데이터 출처     | ALIO(공공기관 경영정보 공개시스템) |
| 원본 형식      | PDF        |
| 파싱 후 형식    | JSON       |
| 분석 단위      | Chunk      |
| 최종 Chunk 수 | `836` |

### 사용 문서

| No. | 문서명   |      Chunk 수 |
| --: | ----- | -----------: |
|   1 | `공간정보보안업무예규` |      `74` |
|   2 | `급여규정` |      `173` |
|   3 | `보안업무예규` |      `174` |
|   4 | `복무규정` |      `67` |
|   5 | `승진임용규칙` |      `78` |
|   6 | `여비규정` |      `56` |
|   7 | `연차_휴가에_관한_예규` |      `30` |
|   8 | `인사규정` |      `184` |
|  합계 |       | `836` |

**분석 결과**

* 전체 `8개`의 규정 문서를 분석 대상으로 사용하였다.
* 파싱 및 청킹 결과 총 `836개`의 Chunk가 생성되었다.
* 문서별 Chunk 수에는 `차이가 있음`을 확인하였다.

---

## 2.2 데이터 구조

RAG 검색 성능 향상을 위해 규정 본문뿐만 아니라 조문 및 문서 구조 정보를 메타데이터로 관리하였다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| article | str | 조문 전체 표기, 조 번호와 조 제목을 함께 저장 |
| paragraph | str | 조문 내 항 번호 |
| content | str | 해당 청크의 실제 원문 내용 |
| document_name | str | 청크가 속한 문서의 이름 |
| chapter | str | 청크가 속한 장 정보 |
| chunk_id | str | 각 청크를 고유하게 식별하기 위한 ID |
| source_file | str | 청크가 생성된 원본 파싱 파일명 |
| raw_content | str | 검색용 전처리 이전의 청크 원문 |
| article_no | str | 조문의 번호 |
| article_title | str | 조문의 제목 |
| section_type | str | 본문과 별표 등 문서 영역을 구분 |
| chunk_type | str | 청크의 분할 단위 또는 유형 |
| appendix_no | str | 별표 청크의 별표 번호 |
| appendix_title | str | 별표의 제목 |
| subsection | str | 별표 내부의 세부 구분 |
| related_article | str | 별표와 연관된 본문 조항 |
| page_content | str | 구조 정보를 원문과 결합한 임베딩·검색용 최종 텍스트 |
| chunk_no | int | 문서 내 청크 순서를 나타내는 번호 |

---
# 3. 데이터 전처리

## 3.1 PDF 파싱 및 구조화

원본 PDF에서 추출된 규정 데이터는 바로 임베딩하지 않고, 규정 문서의 구조를 보존하면서 검색에 적합한 Chunk를 생성하기 위해 다음과 같은 후처리 과정을 수행하였다.

1. **규정 본문과 특수 영역 구분**

   * 부칙, 별표, 별지 등의 시작 위치를 탐지하여 일반 조문 본문과 분리하였다.
   * 일반 조문은 해당 경계 이전의 내용만 본문 Chunk로 사용하였다.

2. **조문 구조화**

   * `제N조`, `제N조의N` 형태를 탐지하여 조 번호(`article_no`)와 조 제목(`article_title`)을 분리하였다.
   * 항 정보가 실제 본문에 존재하는지 확인하여 잘못 인식된 항 정보는 제거하였다.
   * 항이 존재하는 경우 `paragraph`, 그렇지 않은 경우 `article` 유형의 Chunk로 구분하였다.

3. **별표 영역 분리**

   * `[별표 N]`, `[별표 N의 N]` 형태를 탐지하여 별표를 일반 본문과 별도의 Chunk로 생성하였다.
   * 별표 제목과 `(제N조 관련)` 형태의 관련 조문 정보를 추출하여 메타데이터로 저장하였다.

4. **별표 내부 세부 구분 분할**

   * 별표 내부에 `1.`, `2.`, `3.` 등의 번호형 세부 구분이 존재하는 경우 이를 각각 독립적인 Chunk로 분리하였다.
   * 첫 번째 세부 구분 이전에 존재하는 별표 제목 등의 공통 정보는 첫 번째 Chunk에 포함하여 정보가 유실되지 않도록 하였다.

5. **별표 비고 분리**

   * 별표 내부의 `비고:` 영역을 별도로 탐지하였다.
   * 별표의 핵심 본문과 비고를 분리하고, 비고는 `appendix_note` 유형의 독립적인 Chunk로 생성하였다.

6. **메타데이터 생성 및 보완**

   * 각 Chunk에 `document_name`, `chapter`, `article_no`, `article_title`, `paragraph`, `section_type`, `chunk_type`, `appendix_no`, `appendix_title`, `subsection`, `related_article` 등의 구조 정보를 저장하였다.
   * 최종 처리 후 문서별 Chunk 순서에 따라 `chunk_no`와 고유한 `chunk_id`를 다시 부여하였다.

7. **검색 및 임베딩용 최종 텍스트 생성**

   * 정규화된 본문에 문서명, 장, 조, 항, 별표 번호, 별표 제목, 세부 구분 등의 구조 정보를 결합하여 `page_content`를 생성하였다.
   * 최종 `page_content`를 Vector DB 검색 및 임베딩에 활용하였다.


**해석**

원본 파싱 결과에 포함된 일반 조문과 별표 등의 문서 구조를 분리하고, 검색에 필요한 의미 단위로 Chunk를 재구성하였다.

특히 별표 내부의 세부 항목과 비고를 별도의 Chunk로 분리하여 하나의 Chunk에 서로 다른 정보가 과도하게 포함되는 것을 방지하였다. 또한 동일한 본문이나 별표 내용이 반복 저장되지 않도록 중복 제거를 수행하였다.

이를 통해 규정 문서의 구조적 정보를 유지하면서 검색 및 임베딩에 적합한 형태로 데이터를 정제하였다.

---

## 3.2 텍스트 정규화

Chunk 생성 이후 검색 및 임베딩에 불필요한 형식적 요소를 제거하고 문서의 의미 구조를 보존하기 위해 다음과 같은 텍스트 정규화를 수행하였다.

* **HTML 태그 제거**

  * BeautifulSoup을 이용하여 HTML 태그를 제거하고 실제 텍스트만 추출하였다.

* **공백 및 줄바꿈 정규화**

  * 연속된 공백과 탭을 정리하였다.
  * 불필요하게 반복되는 줄바꿈을 제거하여 텍스트 형식을 통일하였다.

* **HTML Table 텍스트 변환**

  * HTML `<table>` 구조를 그대로 제거하지 않고 행과 열의 관계가 유지되도록 검색 가능한 텍스트 형태로 변환하였다.
  * 헤더가 존재하는 표는 `헤더: 값` 형태로 변환하여 각 값의 의미를 보존하였다.
  * 헤더가 없는 표는 각 셀의 값을 구분하여 텍스트 형태로 변환하였다.

* **조문 정보 정규화**

  * `제 N 조`, `제N조`와 같이 공백 표현이 다른 조 번호를 일정한 형태로 정규화하였다.
  * 조 번호와 조 제목을 각각 `article_no`, `article_title`로 분리하여 저장하였다.

* **별표 구조 정규화**

  * 별표 번호, 별표 제목, 관련 조문, 세부 구분을 각각 별도의 메타데이터로 저장하였다.
  * 다른 별표나 별지가 시작되는 지점을 현재 별표의 종료 지점으로 처리하여 서로 다른 영역의 내용이 하나의 Chunk에 섞이지 않도록 하였다.

* **일반 Chunk 중복 제거**

  * `document_name`, `article_no`, `paragraph`, 정규화된 본문을 기준으로 중복 여부를 판단하였다.
  * 동일한 조·항의 동일 본문이 반복되는 경우 최초 Chunk만 유지하였다.

* **별표 Chunk 중복 제거**

  * 문서명, 별표 번호, 별표 제목, 세부 구분 및 정규화된 별표 본문을 기준으로 중복을 제거하였다.
  * 별표의 `비고` 내용 역시 별도의 기준으로 중복 여부를 판단하여 동일 내용의 반복 저장을 방지하였다.

* **RAG 검색용 `page_content` 구성**

  * 정규화된 본문만 임베딩하지 않고 문서의 구조 정보를 함께 포함하였다.
  * 문서명, 장, 조, 항, 별표 번호, 별표 제목, 세부 구분 등의 정보를 본문 앞에 결합하여 최종 검색용 텍스트를 생성하였다.

예를 들어 최종 검색 대상 텍스트는 다음과 같은 구조를 가진다.

```text
문서: 여비규정
장: 제3장 국내여비
조: 제10조(숙박비)
항: ①

[정규화된 실제 규정 본문]
```

별표의 경우 다음과 같이 구조 정보를 함께 유지한다.

```text
문서: 여비규정
별표: 별표 1
별표 제목: 여비 지급기준
구분: 1. 국내여비

[정규화된 별표 내용]
```

이를 통해 단순히 본문 텍스트만 임베딩하는 것보다 **해당 내용이 어느 문서의 어느 조·항 또는 별표에 속하는지에 대한 구조적 문맥을 함께 검색할 수 있도록 구성하였다.**

또한 표 구조 변환과 중복 제거를 통해 검색에 불필요한 노이즈를 줄이고, 동일한 내용이 검색 결과에 반복적으로 나타나는 문제를 최소화하였다.

---


# 4. 최종 RAG 데이터 구성

분석 및 전처리를 완료한 데이터를 최종적으로 다음 구조의 RAG 파이프라인에 적용하였다.

```text
사내 규정 PDF
      ↓
PDF Parsing
      ↓
규정 구조 분석
(장 / 조 / 항 / 별표)
      ↓
Chunk 생성
      ↓
정규화 및 중복 제거
      ↓
Embedding
      ↓
ChromaDB
      ↓
Similarity Retrieval
      ↓
Neighbor Chunk 확장
      ↓
Context 생성
      ↓
LLM
      ↓
최종 답변 + 출처
```

### 최종 데이터

```text
최종 Chunk 수     : 836
Embedding Model   : jhgan/ko-sroberta-multitask
Vector DB         : ChromaDB
Collection        : company_regulations
검색 Top-K         :10
Neighbor Window   : ±1
Generation Model  : `gpt-4o-mini`
```

---

# 5. 데이터 분석 결론

데이터 분석을 통해 사내 규정 데이터는 문서마다 길이와 구조, 조문 구성, 주요 용어에 차이가 존재한다는 것을 확인하였다.

이러한 분석 결과를 기반으로 단순한 고정 길이 텍스트 분할 대신 **규정의 장·조·항 구조를 보존하는 청킹 방식**을 적용하였다. 또한 검색된 Chunk만으로 문맥이 부족할 가능성을 고려하여 **인접 Chunk 확장 방식**을 적용하였다.

최종적으로 전처리된 문서를 임베딩하여 ChromaDB에 적재하고, 검색 결과를 LLM의 Context로 전달하는 RAG 파이프라인을 구축하였다.

---

# 6. 검색 품질 평가

데이터 분석 이후 구축된 RAG 파이프라인의 실제 검색 성능은 별도의 **85문항의 검색 품질 평가 데이터셋**을 통해 검증하였다.

평가에서는 다음 지표를 활용한다.

* Hit@K
* Precision@K
* Recall@K
* MRR@K

이를 통해 데이터 분석 단계에서 수립한 청킹 및 검색 전략이 실제 질문 검색 성능 향상으로 이어지는지 확인하였다.


## 6.1 평가 데이터셋 구성

검색 품질 평가는 총 **85개 질문**으로 구성된 별도의 평가 데이터셋을 활용하였다.

본 프로젝트에서는 실제 사내 챗봇의 사용자 질문 데이터가 존재하지 않아, 어떤 규정에 대해 어떤 유형의 질문이 실제로 많이 발생하는지 객관적인 빈도를 직접 확인하기 어려웠다.

이에 따라 평가 질문은 다음 두 가지를 함께 활용하여 구성하였다.

* 프로젝트 수행 과정에서 확보한 **규정 문서에 대한 도메인 이해**
* 규정 문서 전체를 AI에 제공한 뒤, **사용자가 실제로 물어볼 가능성이 높은 질문을 예상 빈도에 따라 생성**하도록 한 결과

질문 생성 시 특정 규정이나 특정 주제에 과도하게 편중되지 않도록 전체 규정 문서를 기준으로 비교적 균형 있게 질문을 구성하되, 실제 사용 상황에서 자주 문의될 가능성이 높은 내용은 상대적으로 더 많이 포함하였다.

이를 통해 실제 사용자 로그가 없는 상황에서도 단순 무작위 질문이 아니라, **규정의 내용과 예상 사용자 관심도를 함께 반영한 평가 데이터셋**을 구축하고자 하였다.

최종적으로 생성된 85개 질문에 대해서는 이후 별도의 의미 분석 및 군집화를 수행하여 질문들이 어떤 주제 영역으로 분포하는지 확인하였다.

즉, `휴가·연차`, `보안·자료`, `급여·수당`, `출장·여비`, `인사·복무` 등의 대표 Topic은 질문 생성 단계에서 미리 고정한 분류가 아니라, **평가 질문 생성 이후 의미적 특성을 분석하는 과정에서 정리한 대표 주제**이다.

이와 같은 방법은 실제 사용자 질문 데이터가 없는 초기 서비스 개발 단계에서 현실적인 평가셋을 구성하기 위한 대안으로 활용하였으며, 향후 실제 서비스 운영을 통해 사용자 질문 로그가 축적될 경우 이를 기반으로 평가 데이터의 주제 비중과 난이도를 재조정할 필요가 있다.

### 정답 Chunk(Ground Truth) 구성

생성된 각 평가 질문에 대해 해당 질문의 답변 근거가 되는 규정 조항을
원문과 대조하여 정답 Chunk ID(Gold Chunk)를 지정하였다.

하나의 질문에 여러 조항이 동시에 관련되는 경우에는 복수의 Chunk를
정답으로 지정하여 단일 정답만을 가정하지 않도록 구성하였다.

이후 Retriever가 반환한 Top-K 결과와 Gold Chunk를 비교하여
Hit@K, Precision@K, Recall@K, MRR@K를 계산하였다.

---

## 6.2 평가 질문의 의미적 분포

평가 질문들이 실제로 서로 다른 의미 영역을 구성하고 있는지 확인하기 위해 질문 임베딩을 UMAP으로 2차원 축소하여 시각화하였다.

### 분석 결과

* `보안·자료` 질문은 다른 Topic과 비교하여 비교적 독립적인 영역에 군집을 형성하였다.
* `출장·여비` 역시 상단 영역에서 비교적 명확한 군집을 형성하였다.
* `휴가·연차`, `급여·수당` 질문은 일부 영역에서 서로 가깝게 분포하였다.
* `인사·복무` 질문은 문항 수가 적어 작은 군집 형태로 나타났다.

이를 통해 평가 데이터가 단일한 질문 유형으로 구성된 것이 아니라, **서로 다른 규정 영역에 대한 다양한 의미적 질문을 포함하고 있음**을 확인하였다.

다만 UMAP은 고차원 임베딩을 시각적으로 확인하기 위한 차원 축소 방법이므로, 군집 간 거리 자체를 검색 성능의 직접적인 지표로 해석하지 않고 평가 데이터의 전반적인 분포를 확인하는 용도로 활용하였다.

---

## 6.3 Retriever Top-K 비교 실험

Retriever가 반환하는 문서 수인 `K` 값에 따라 검색 품질이 어떻게 변화하는지 확인하기 위해 `K=3`부터 `K=10`까지 비교 실험을 수행하였다.

### 실험 결과

|  K | Hit@K | Precision@K | Recall@K | MRR@K |
| -: | ----: | ----------: | -------: | ----: |
|  3 | 0.788 |       0.267 |    0.771 | 0.700 |
|  4 | 0.812 |       0.209 |    0.800 | 0.706 |
|  5 | 0.847 |       0.174 |    0.835 | 0.713 |
|  6 | 0.859 |       0.147 |    0.847 | 0.715 |
|  7 | 0.871 |       0.128 |    0.859 | 0.717 |
|  8 | 0.882 |       0.113 |    0.871 | 0.718 |
|  9 | 0.894 |       0.102 |    0.882 | 0.719 |
| 10 | 0.894 |       0.092 |    0.882 | 0.719 |

### 분석

`K`가 증가할수록 **Hit@K와 Recall@K는 지속적으로 증가**하는 경향을 보였다.

특히 `K=3`과 `K=10`을 비교하면 다음과 같다.

* Hit@K: `0.788 → 0.894`
* Recall@K: `0.771 → 0.882`
* MRR@K: `0.700 → 0.719`

즉, 검색 후보의 수를 증가시킬수록 정답 Chunk를 검색 결과에 포함할 가능성이 높아졌다.

반면 Precision@K는 다음과 같이 감소하였다.

* Precision@3: `0.267`
* Precision@5: `0.174`
* Precision@10: `0.092`

이는 K가 증가하면서 정답 Chunk뿐만 아니라 관련성이 상대적으로 낮은 Chunk도 함께 반환되기 때문이다.

본 실험에서는 Precision 단독 최대화보다는 **RAG 답변 생성에 필요한 정답 근거를 검색 단계에서 놓치지 않는 것**을 더 중요하게 판단하였다.

하지만, K값의 상승에 따른 챗봇 로딩 시간이나 답변 시간의 trade-off를 고려하기 위해 k값별, 모델별 답변생성시간을 측정하였고 이는 후술하였다.

---

## 6.4 K 값에 따른 검색 성능 해석

실험 결과 `K=3`에서는 Precision이 가장 높았으나, Hit@K와 Recall@K가 상대적으로 낮아 정답 근거가 검색 결과에 포함되지 않을 가능성이 존재하였다.

반대로 K 값을 증가시키면서 Recall은 지속적으로 개선되었으며, 특히 `K=9`와 `K=10`에서 다음과 같이 가장 높은 성능을 기록하였다.

```text
Hit@K   : 0.894
Recall  : 0.882
MRR     : 0.719
```

`K=9`에서 이미 Hit, Recall, MRR이 최대 수준에 도달하였으며 `K=10`에서는 동일한 검색 성능이 유지되었다.

따라서 K를 과도하게 증가시키는 것은 검색 품질 개선 효과가 제한적인 반면, LLM에 전달되는 Context의 양을 증가시킬 수 있다는 Trade-off가 존재한다.

본 프로젝트에서는 **정답 근거 누락을 최소화하는 것을 우선시하여 최대 Recall 구간에 해당하는 높은 K 값을 활용하고**, 이후 window를 활용한 인접 Chunk 확장 및 Context 구성 과정에서 문맥을 보완하는 전략을 적용하였다.

---

## 6.5 모델 및 Top-K별 응답 시간 비교

검색 품질뿐만 아니라 실제 Streamlit 챗봇에서의 사용성을 고려하기 위해 생성 모델과 Top-K 조합별 응답 시간을 추가로 측정하였다.

비교 대상은 `gpt-4o-mini`와 `gpt-5-mini`이며 검색 시간, 답변 생성 시간 및 전체 응답 시간을 각각 측정하였다.

### 실험 결과

| Model       |  K | 평균 검색 시간 | 평균 답변 생성 시간 | 평균 전체 응답 시간 |
| ----------- | -: | -------: | ----------: | ----------: |
| gpt-4o-mini |  5 |   4.264초 |      1.383초 |      5.649초 |
| gpt-4o-mini |  6 |   3.698초 |      1.365초 |      5.066초 |
| gpt-4o-mini | 10 |   3.687초 |      1.496초 |      5.187초 |
| gpt-5-mini  |  5 |   3.664초 |     10.960초 |     14.627초 |
| gpt-5-mini  |  6 |   4.133초 |     10.964초 |     15.099초 |

---

## 6.6 검색 시간 비교

Retriever의 평균 검색 시간은 대부분 약 **3.6~4.3초** 범위에서 나타났다.

`gpt-4o-mini / K=5` 조합에서 4.264초로 다소 높게 측정되었지만, 나머지 조합에서는 큰 차이가 나타나지 않았다.

특히 `gpt-4o-mini` 기준으로 다음과 같은 결과를 보였다.

```text
K=5  : 4.264초
K=6  : 3.698초
K=10 : 3.687초
```

K가 증가했다고 해서 검색 시간이 선형적으로 증가하지는 않았다.

이는 검색 시간 측정값에 실행 환경, 캐시 상태 및 네트워크 등의 영향을 받을 수 있기 때문에, 해당 실험에서는 개별 수치의 작은 차이보다는 **K를 증가시켜도 검색 지연이 크게 증가하지 않았다는 점**을 중심으로 해석하였다.

---

## 6.7 모델별 답변 생성 시간 비교

모델에 따른 가장 큰 차이는 **답변 생성 시간**에서 나타났다.

`gpt-4o-mini`의 경우 평균 답변 생성 시간이 약 **1.36~1.50초** 수준으로 나타났다.

반면 `gpt-5-mini`는 약 **10.96초**가 소요되었다.

```text
gpt-4o-mini : 약 1.4초
gpt-5-mini  : 약 11.0초
```

따라서 본 실험 환경에서는 `gpt-5-mini`가 `gpt-4o-mini`보다 답변 생성 단계에서 약 7~8배가량 긴 시간이 소요되었다.

검색 시간 자체에는 모델별 큰 차이가 없었으므로, 전체 응답 시간 차이의 주된 원인은 **LLM 답변 생성 시간**으로 판단하였다.

---

## 6.8 전체 응답 시간 비교

평균 전체 응답 시간은 다음과 같이 나타났다.

```text
gpt-4o-mini / K=6  : 5.066초
gpt-4o-mini / K=10 : 5.187초
gpt-4o-mini / K=5  : 5.649초

gpt-5-mini / K=5   : 14.627초
gpt-5-mini / K=6   : 15.099초
```

`gpt-4o-mini` 조합은 약 5초 내외의 평균 응답 시간을 보인 반면, `gpt-5-mini` 조합은 약 15초가 소요되었다.

특히 `gpt-4o-mini / K=6`과 `gpt-4o-mini / K=10`의 전체 응답 시간 차이는 약 **0.12초**에 불과하였다.

따라서 Retriever에서 더 많은 후보 문서를 가져오더라도 실제 전체 서비스 응답 시간에는 큰 차이가 발생하지 않았으며, 검색 Recall을 높이기 위해 높은 K 값을 사용하는 것이 가능하다고 판단하였다.

---

## 6.9 최종 설정 선정

검색 품질과 서비스 응답 속도를 종합적으로 고려한 결과 다음과 같은 Trade-off를 확인하였다.

| 항목              | 낮은 K     | 높은 K    |
| --------------- | -------- | ------- |
| Precision       | 높음       | 낮음      |
| Recall          | 낮음       | 높음      |
| 정답 Chunk 누락 가능성 | 상대적으로 높음 | 낮음      |
| Context 양       | 적음       | 많음      |
| 검색 시간           | 큰 차이 없음  | 큰 차이 없음 |

본 프로젝트는 규정 기반 RAG 시스템으로, 관련 없는 문서를 일부 포함하는 것보다 **답변의 근거가 되는 규정 Chunk를 검색 단계에서 누락하는 문제를 더 중요하게 판단하였다.**

이에 따라 Retriever의 K 값을 높은 수준으로 설정하고, 검색된 Chunk와 인접 Chunk를 함께 활용하여 LLM에 충분한 문맥을 제공하도록 구성하였다.

또한 응답 속도 비교 결과 `gpt-4o-mini`가 약 5초 수준의 전체 응답 시간을 기록하여 `gpt-5-mini`보다 시연 및 실제 사용자 인터랙션 측면에서 유리하였다.
하지만, 'gpt-4o-mini'와 'gpt-5-mini'의 답변 성능을 정성적으로 평가한 결과 상위 모델인 'gpt-5-mini'가 답변의 품질을 유의미하게 개선하지 않는다고 판단하여 최종적으로 'gpt-4o-mini'를 선정하였다.

따라서 최종 서비스에서는 **검색 Recall과 실시간 응답성을 함께 확보할 수 있는 조합을 우선적으로 적용하였다.**


---

## 6.10 검색 품질 평가 결론

총 85개의 평가 질문을 이용한 검색 품질 평가 결과, Retriever의 K 값을 증가시킬수록 정답 Chunk를 검색 결과에 포함하는 **Hit@K와 Recall@K가 향상**되는 것을 확인하였다.

`K=3`에서는 Hit@K가 `0.788`, Recall@K가 `0.771`이었으나, `K=9~10`에서는 각각 `0.894`, `0.882`까지 상승하였다.

반면 Precision은 K 증가에 따라 감소하였으며, 이는 더 많은 검색 후보가 반환되면서 비정답 Chunk도 함께 포함되기 때문이다.

본 프로젝트에서는 RAG 특성상 **검색 단계에서 정답 근거가 누락되면 이후 LLM이 올바른 답변을 생성하기 어렵다는 점**을 고려하여 Precision보다 Recall과 Hit@K를 우선하였다.

또한 모델 및 Top-K별 응답 시간을 비교한 결과 `gpt-4o-mini`가 약 5초 수준의 응답 시간을 보였으며, 높은 K 값을 사용하더라도 전체 응답 시간이 크게 증가하지 않는 것을 확인하였다.

따라서 최종 RAG 파이프라인은 다음 방향으로 구성하였다.

* 규정 구조를 보존한 Chunking 적용
* 높은 Recall을 확보할 수 있는 Retriever Top-K 적용
* 검색된 Chunk 주변의 인접 Chunk를 추가하여 Context 보완
* 빠른 응답성을 고려한 생성 모델 적용
* 검색된 근거 문서를 기반으로 최종 답변 생성

이를 통해 **검색 정확도뿐만 아니라 정답 근거의 포괄성과 실제 서비스 응답 속도를 함께 고려한 RAG 파이프라인을 구성하였다.**

## 6.11 검색 실패 사례 분석

Top-10 검색에서도 정답 Chunk가 검색되지 않은 질문을 확인하여
주요 실패 원인을 분석하였다.

- 질문 표현과 규정 원문의 용어 차이
- 답변 근거가 여러 Chunk에 분산된 경우
- 표·별표에 정답이 존재하는 경우
- 서로 유사한 조문이 여러 규정에 존재하는 경우
- 평가 데이터의 Gold Chunk 지정 범위가 지나치게 좁은 경우

이를 통해 단순히 K 값을 증가시키는 것만으로 해결하기 어려운
검색 실패가 존재함을 확인하였다.

### 한계 및 향후 개선

## 6-11-1. 한계점(개선할 점)
#### 사례 1 — 조건부 출장비 질문

> **질문 (Q077)**  
> 외부기관에서 출장비를 일부 지원받으면 회사 출장비도 전액 받을 수 있나요?

**평가 결과**

| 지표 | 결과 |
| --- | ---: |
| Hit@10 | 0 |
| Recall@10 | 0 |
| MRR@10 | 0 |

**분석**

단순한 출장비 금액 질문이 아니라 **`외부기관 지원`이라는 예외 조건이 포함된 질문**으로, Top-10 검색 결과에서 정답 Chunk를 찾지 못하였다.

일반적인 출장·여비 관련 Chunk는 검색되었으나, 질문에서 요구하는 **세부 예외 조건과 직접적으로 관련된 규정을 검색 단계에서 충분히 구분하지 못한 것**으로 판단된다.

**개선 방향**

- 예외·조건 관련 조항을 별도의 **Metadata**로 구분
- 질문의 세부 조건과 Chunk 간 관련성을 다시 평가하는 **Reranker 적용**
- 일반적인 여비 규정보다 `외부기관 지원`과 같이 **구체적인 조건이 일치하는 Chunk가 우선 검색되도록 검색 전략 개선**

이를 통해 일반 규정 검색뿐만 아니라 **예외 조건이 포함된 복합 질문에 대한 검색 정확도**를 높일 필요가 있다.


#### 사례 2 — 조건부 출장비 질문

추천 질문인 **“보상휴가는 어떻게 지급되나요?”**를 입력했지만, Retriever가 관련 규정을 검색하지 못해 챗봇이 답변을 생성하지 못했다.

### 원인 분석

추천 질문을 등록하는 과정에서 **실제 검색 및 답변 가능 여부를 사전에 검증하지 않은 것**이 주요 원인이었다.

또한 다음과 같은 가능성을 확인할 필요가 있다.

- 사용자 질문의 **‘보상휴가’ 표현과 규정 원문의 용어가 다른 경우**
- 관련 내용이 **표·별표에 포함되어 파싱 또는 청킹 과정에서 누락된 경우**
- 관련 Chunk가 생성되었지만 **ChromaDB 적재 또는 검색 과정에서 정상적으로 검색되지 않은 경우**

### 개선 방향

- **Golden Set을 활용해 실제 검색 및 답변이 가능한 질문만 추천 질문으로 등록**
- 사용자 표현과 규정 원문의 용어 차이를 보완하기 위한 **동의어 기반 Query Rewrite 적용**
- 관련 조항·표·별표가 **파싱 및 청킹 과정에서 정상적으로 생성되었는지 검증**
- 생성된 Chunk가 **ChromaDB에 정상적으로 적재되고 검색되는지 점검**

이를 통해 추천 질문의 신뢰도를 높이고, **사용자 표현과 규정 원문의 용어 차이로 발생하는 검색 실패를 줄일 필요가 있다.**

**개선 사항**

본 프로젝트의 평가 데이터셋은 실제 서비스 사용자 로그가 존재하지 않는
상황에서 규정 문서와 도메인 지식, AI를 활용하여 구축하였다.

따라서 실제 사용자 질문의 표현 방식과 주제별 발생 빈도를 완전히
반영한다고 보기에는 한계가 있다.

향후 실제 사용자 질문 데이터가 축적될 경우 이를 기반으로 평가 데이터셋을
재구성하고, 검색 실패 사례를 중심으로 다음 개선을 검토할 수 있다.

- 실제 사용자 질문 기반 평가셋 재구성
- Embedding 모델 비교
- Hybrid Search 적용
- Reranking 적용
- Query Rewrite 성능 검증
- Neighbor Window 크기 비교